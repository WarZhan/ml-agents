# Unity ML-Agents Toolkit 项目概述与技术报告

> 本文档基于对仓库源码的通读整理，重点分析 `Project/Assets/ML-Agents/Examples` 下的示例（Demo），并梳理整个项目的代码框架与核心组件。

---

## 一、项目简介

**Unity ML-Agents Toolkit** 是 Unity 官方开源的机器学习训练框架，目标是让游戏与仿真环境成为训练智能体（Agent）的"训练场"。它将 **Unity 游戏引擎（C# 运行时）** 与 **基于 PyTorch 的 Python 训练后端** 通过 gRPC 双向打通，支持：

- 单智能体 / 多智能体协作 / 多智能体对抗（自博弈 Self-Play）
- 深度强化学习算法：**PPO、SAC、MA-POCA**
- 模仿学习：**BC（行为克隆）、GAIL**
- 课程学习（Curriculum Learning）、环境随机化（Domain Randomization）
- 通过 **Inference Engine（Sentis）** 在 Unity 内做跨平台本地推理（ONNX 模型）
- Python LLAPI / Gym / PettingZoo 封装

当前对应版本：**Release 23（Python 包 `mlagents` 1.1.0，Unity 包 `com.unity.ml-agents` 4.0.0）**，License 为 Apache-2.0。

---

## 二、仓库总体结构

```
ml-agents/
├── Project/                         # Unity 示例工程（本报告分析重点）
│   └── Assets/ML-Agents/Examples/   # 18 个示例环境（Demo）
├── com.unity.ml-agents/             # Unity SDK 包（C# 运行时核心）
│   └── Runtime/                     # Agent / Academy / Sensor / Actuator / Policy / Grpc ...
├── com.unity.ml-agents.extensions/  # 扩展包（物理传感器、Match3 集成等）
├── ml-agents/mlagents/              # Python 训练库（trainers）
│   └── trainers/                    # ppo / sac / poca / ghost(self-play) ...
├── ml-agents-envs/                  # Python 与 Unity 通信的底层 API（LLAPI、gRPC）
├── config/                          # 训练超参数 YAML 配置
│   ├── ppo/  sac/  poca/  imitation/
├── docs/                            # 文档
└── Readme.md
```

### 三层架构

| 层 | 位置 | 职责 |
|----|------|------|
| **Unity 运行时（C#）** | `com.unity.ml-agents/Runtime` | 智能体生命周期、观测采集、动作执行、本地推理 |
| **通信层（gRPC）** | `Runtime/Grpc` + `ml-agents-envs` | Unity ↔ Python 的观测/动作/奖励双向传输 |
| **训练后端（Python/PyTorch）** | `ml-agents/mlagents/trainers` | 算法实现、神经网络、经验回放、模型导出（ONNX） |

---

## 三、核心框架（C# 运行时核心组件）

所有示例都建立在以下核心抽象之上（位于 `com.unity.ml-agents/Runtime`）：

### 1. `Academy`（单例，全局调度器）
- 全局单例 `Academy.Instance`，驱动所有 Agent 的决策节拍（`EnvironmentStep`）。
- 管理通信器开关 `IsCommunicatorOn`（判断是否连接 Python 训练）。
- 提供 `EnvironmentParameters`（环境参数，用于课程学习/随机化）、`StatsRecorder`（自定义指标上报）。

### 2. `Agent`（智能体基类）— 最核心的开发入口
开发者通过继承 `Agent` 并重写以下生命周期方法来定义一个任务：

| 方法 | 作用 | 调用时机 |
|------|------|---------|
| `Initialize()` | 初始化（缓存组件、读取参数） | 启动一次 |
| `OnEpisodeBegin()` | 重置场景/智能体状态 | 每个回合开始 |
| `CollectObservations(VectorSensor)` | 写入向量观测 | 每次决策前 |
| `OnActionReceived(ActionBuffers)` | 接收动作、施加到环境、给奖励 | 每个决策步 |
| `WriteDiscreteActionMask(...)` | 动作掩码（屏蔽非法离散动作） | 每次决策前 |
| `Heuristic(ActionBuffers)` | 人工/键盘控制（调试与录制示范） | 启发式模式 |

关键 API：`RequestDecision()`、`AddReward()/SetReward()`、`EndEpisode()`、`EpisodeInterrupted()`、`MaxStep`、`StepCount`。

### 3. 观测系统（Sensors）
- **`VectorSensor`**：数值向量观测（最常用，见 3DBall）。
- **`RayPerceptionSensorComponent3D/2D`**：射线感知（类似"激光雷达"，检测带 tag 的物体，Soccer/Hallway/PushBlock 常用）。
- **`CameraSensor` / `RenderTextureSensor`**：视觉观测（卷积编码，Visual* 场景）。
- **`GridSensor`**：网格俯视观测。
- **`BufferSensorComponent`**：**变长观测**（可观测不定数量的实体，见 Sorter）。
- **`StackingSensor`**：堆叠历史帧（提供短期记忆）。
- 可继承 `SensorComponent`/`SensorBase` 自定义传感器（见 Basic 示例）。

### 4. 动作系统（Actuators）
- **`ActionSpec`**：定义动作空间，支持 **连续（Continuous）** 与 **离散（Discrete，可多分支）** 混合。
- **`ActionBuffers`**：`ContinuousActions` / `DiscreteActions`。
- 可继承 `ActuatorComponent`/`IActuator` 自定义执行器（见 Basic、Match3 示例）。

### 5. 策略系统（Policies）— `BehaviorParameters`
挂在 Agent GameObject 上的核心组件，决定动作来源：
- **Default**：连 Python 则远程训练，否则用内置模型推理。
- **HeuristicOnly**：用 `Heuristic()` 手动控制。
- **InferenceOnly**：用 ONNX 模型本地推理。
- 字段 `BehaviorName`（对应 config YAML 的 behavior 名）、`TeamId`（自博弈分队）。

### 6. 多智能体组 `SimpleMultiAgentGroup`（MA-POCA 核心）
- 把多个 Agent 注册为"队伍"，提供 `AddGroupReward()`、`EndGroupEpisode()`、`GroupEpisodeInterrupted()`。
- 用于**协作**（DungeonEscape）与**对抗团队**（Soccer），配合 Python 端 `poca` trainer。

### 7. 示范与通信
- **`DemonstrationRecorder`**：录制 `.demo` 示范文件，供 BC/GAIL 模仿学习。
- **`Grpc/CommunicatorObjects`**：Protobuf 生成的通信消息体；`RpcCommunicator` 负责与 Python 收发。
- **SideChannels**：`EnvironmentParametersChannel`、`StatsSideChannel`、`EngineConfigurationChannel` 等旁路通道。

---

## 四、Examples 示例（Demo）详细分析

`Project/Assets/ML-Agents/Examples` 下共 18 个目录，其中 `SharedAssets` 为公共脚本/素材，`Startup` 为启动器。下面按"难度/模式"分类逐一剖析。

### 公共基础设施：`SharedAssets/Scripts`
被多个示例复用的核心工具脚本：

| 脚本 | 作用 |
|------|------|
| `JointDriveController.cs` | **关节驱动控制器**，封装 `ConfigurableJoint`，提供 `SetupBodyPart`、`SetJointTargetRotation`、`SetJointStrength`——所有 Ragdoll 运动（Crawler/Walker/Worm）的基础 |
| `OrientationCubeController.cs` | 提供稳定的"模型空间参考朝向"，避免 Ragdoll 抖动干扰观测 |
| `GroundContact.cs` / `TargetContact.cs` / `CollisionCallbacks.cs` | 碰撞/接地检测回调 |
| `DirectionIndicator.cs` | 指向目标的方向指示器（可视化） |
| `ModelOverrider.cs` | CI/命令行下覆盖 ONNX 模型（自动化测试） |
| `ProjectSettingsOverrides.cs` | 训练时统一物理/时间步设置 |
| `SensorBase.cs` | 自定义传感器基类 |

---

### 1. 3DBall — 平衡球（入门基准）
- **文件**：`3DBall/Scripts/Ball3DAgent.cs`、`Ball3DHardAgent.cs`
- **任务**：转动平台，使球不掉落。
- **观测**：平台 x/z 旋转、球相对位置、球速度（8 维向量观测）。`Hard` 版改用堆叠观测、无球速度，更难。
- **动作**：2 维**连续**（绕 X/Z 轴旋转）。
- **奖励**：每步存活 +0.1；球掉落 -1 并结束回合。
- **重置**：随机平台倾角与球位置；支持环境参数 `mass`/`scale` 做随机化。
- **训练**：PPO/SAC，`config/ppo/3DBall.yaml`（`max_steps=500000`，2 层 128 单元 MLP）。
- **教学价值**：最简连续控制范式，演示 `CollectObservations / OnActionReceived / OnEpisodeBegin / Heuristic` 四件套。

### 2. Basic — 一维寻路（最小可用示例）
- **文件**：`Basic/Scripts/BasicController.cs`、`BasicSensorComponent.cs`、`BasicActuatorComponent.cs`
- **特殊性**：**不继承 `Agent`**，而是用 `MonoBehaviour` + **自定义 Sensor + 自定义 Actuator** 组合，演示 ML-Agents 的"组件化"用法。
- **任务**：在一维线段上左右移动，找到较大奖励的目标。
- **观测**：位置的 one-hot 编码（`BasicSensor.WriteObservation`）。
- **动作**：离散 3 选 1（左/不动/右，`ActionSpec.MakeDiscrete(3)`）。
- **奖励**：每步 -0.01；小目标 +0.1，大目标 +1。
- **教学价值**：展示如何**脱离 Agent 基类**，自定义 `SensorComponent` 与 `ActuatorComponent`，以及手动注册 `RpcCommunicator`。

### 3. GridWorld — 网格世界（离散 + 动作掩码 + 目标条件）
- **文件**：`GridWorld/Scripts/GridAgent.cs`、`GridArea.cs`
- **任务**：在网格中走到指定符号（绿+ 或 红×），目标由 `GridGoal` 随机指定。
- **观测**：视觉观测（俯视相机）+ 目标的 one-hot（`VectorSensorComponent` 作为 GoalSensor，体现 **goal-conditioned**）。
- **动作**：离散 5 选 1（不动/上/下/左/右）。
- **核心亮点**：**动作掩码** `WriteDiscreteActionMask`——在边界处屏蔽会撞墙的动作（`SetActionEnabled`）。
- **奖励**：每步 -0.01；到达正确目标 +1，错误目标 -1。
- **决策节拍**：通过 `timeBetweenDecisionsAtInference` + 手动 `RequestDecision()` 控制推理频率，并强制相机 `Render()` 以保证视觉观测最新。

### 4. PushBlock — 推箱子
- **文件**：`PushBlock/Scripts/PushAgentBasic.cs`、`PushBlockEnvController.cs`、`GoalDetect.cs`
- **任务**：将方块推到目标区域。
- **观测**：射线感知（`RayPerceptionSensor`，在 Prefab 中配置）。
- **动作**：离散 7 选 1（前后、左右平移、左右旋转）。
- **奖励**：每步 `-1/MaxStep` 时间惩罚；箱子入区 +5（`GoalDetect` 触发 `ScoredAgent`）并切换地面材质做视觉反馈。
- **变体**：`PushAgentCollab`（协作多智能体）、`PushBlockWithInput`（接入新版 Input System，演示 `InputActuator`）。
- **环境参数**：`dynamic_friction`、`block_scale`、`block_drag` 支持随机化。

### 5. PushBlockWithInput — Input System 集成
- **文件**：`PushBlockWithInput/Scripts/*`（`PushBlockActions`、`PushBlockWithInputPlayerController` 等）
- **亮点**：演示如何把 Unity **新输入系统（Input System）** 的 `InputActionAsset` 通过 `IInputActionAssetProvider` 桥接到 ML-Agents 的 `InputActuator`，实现"人玩"与"AI 控"共用同一套操作映射。

### 6. Hallway — 走廊（记忆任务）
- **文件**：`Hallway/Scripts/HallwayAgent.cs`
- **任务**：先观察房间中央的符号（O 或 X），再走到对应符号的目标处。**需要记忆**之前看到的符号。
- **观测**：射线感知 + `StepCount/MaxStep`；依赖 **`StackingSensor` / LSTM（memory）** 记住先前观测。
- **动作**：离散 5 选 1。
- **奖励**：每步 `-1/MaxStep`；选对 +1、选错 -0.1，回合结束。
- **亮点**：使用 `StatsRecorder` 上报 `Goal/Correct`、`Goal/Wrong` 自定义指标；训练 config 通常开启 `memory`（循环网络）。

### 7. Pyramids — 金字塔（稀疏奖励 + 好奇心）
- **文件**：`Pyramids/Scripts/PyramidAgent.cs`、`PyramidSwitch.cs`、`PyramidArea.cs`
- **任务**：先碰开关（点亮），开关触发后生成金字塔，再撞倒金字塔顶取得金砖。
- **观测**：开关状态 + 自身速度 + 射线感知。
- **动作**：离散 5 选 1。
- **奖励**：**稀疏**——仅到达目标 +2，每步 `-1/MaxStep`。
- **亮点**：典型的**稀疏奖励**场景，配套 `config/ppo/PyramidsRND.yaml`（**RND 随机网络蒸馏**）/ 好奇心（Curiosity）内在奖励解决探索难题。

### 8. FoodCollector — 采集食物（多智能体 + 混合动作）
- **文件**：`FoodCollector/Scripts/FoodCollectorAgent.cs`、`FoodCollectorArea.cs`、`FoodLogic.cs`
- **任务**：多个智能体在同一区域抢食物（好食物 +1、坏食物 -1），可发射激光"冻结"对手。
- **观测**：局部速度 + 冻结/射击状态 + 射线感知（可选视觉版 `VisualFoodCollector`）。
- **动作**：**混合动作**——3 维连续（前后/左右/转向）+ 1 维离散（是否射击）。
- **奖励**：吃到好/坏食物 ±1；可向全局 `totalScore` 贡献。
- **亮点**：演示**连续+离散混合动作空间**、多智能体竞争、状态机（冻结/中毒/饱食的材质切换与计时）。

### 9. WallJump — 跳墙（课程学习 + 多模型切换）
- **文件**：`WallJump/Scripts/WallJumpAgent.cs`
- **任务**：跳过不同高度的墙到达目标；高墙需要先推矮箱子当垫脚。
- **观测**：相对位置 + 接地检测；射线感知。
- **动作**：离散 4 分支（前后 / 旋转 / 左右 / 跳）。
- **核心亮点 1 — 课程学习**：`config/ppo/WallJump_curriculum.yaml` 用 `environment_parameters` 定义 `big_wall_height`/`small_wall_height` 的多个 Lesson，按训练进度（`progress` 阈值）逐步加高墙体。
- **核心亮点 2 — 多模型**：根据墙体配置（无墙/小墙/大墙）在运行时 `SetModel()` 切换 `noWallBrain/smallWallBrain/bigWallBrain` 三个不同 ONNX 模型，体现分层/分场景策略。

### 10. Crawler — 四足爬行（连续运动控制）
- **文件**：`Crawler/Scripts/CrawlerAgent.cs`（依赖 `JointDriveController`、`OrientationCubeController`）
- **任务**：控制四足 Ragdoll 朝目标行走并匹配目标速度。
- **观测**：朝向立方体参考系下的速度误差、各身体部位接地状态与关节力、目标相对位置、向下射线测高。
- **动作**：**高维连续**——每条腿的关节目标旋转 + 关节强度（约 20 维）。
- **奖励**：`匹配速度奖励 × 朝向目标奖励`（连续塑形奖励，sigmoid 衰减曲线）。
- **亮点**：物理 Ragdoll 控制范式，`OrientationCube` 稳定参考系是训练成功的关键技巧；有 Dynamic/Static/VariableSpeed 多种变体。

### 11. Walker — 双足人形行走
- **文件**：`Walker/Scripts/WalkerAgent.cs`
- 与 Crawler 同源（同样基于 `JointDriveController`），但身体部位更多、自由度更高，是更难的人形运动控制基准。可通过环境参数调节目标速度、重力等。

### 12. Worm — 蠕虫运动
- **文件**：`Worm/Scripts/WormAgent.cs`
- 三段式身体的连续关节控制，是介于 3DBall 与 Crawler 之间的运动控制示例。

### 13. Soccer（SoccerTwos / StrikersVsGoalie）— 足球（自博弈 + 团队对抗）
- **文件**：`Soccer/Scripts/AgentSoccer.cs`、`SoccerEnvController.cs`、`SoccerBallController.cs`
- **任务**：蓝队 vs 紫队踢球进对方球门。
- **结构**：`SoccerEnvController` 用两个 `SimpleMultiAgentGroup`（蓝/紫）管理团队，进球时给整队 `AddGroupReward`（赢队 `1 - 用时比例`，输队 -1）后 `EndGroupEpisode`。
- **角色**：`Striker`（前锋，带存活惩罚催促进攻）/ `Goalie`（守门员，带存活奖励）/ `Generic`，速度参数不同。
- **观测**：射线感知（不同队 tag 顺序不同，区分己方/对方）。
- **动作**：离散 3 分支（前后/左右/旋转）；碰球时按 `m_KickPower` 施加踢球力。
- **核心亮点 — 自博弈（Self-Play）**：`BehaviorParameters.TeamId` 区分队伍，Python 端 `ghost` trainer + `poca`，对手为历史版本快照（`config/poca/SoccerTwos.yaml` 含 `self_play` 配置）。`ball_touch` 通过课程逐步减小引导奖励。

### 14. DungeonEscape — 地牢逃脱（协作 MA-POCA）
- **文件**：`DungeonEscape/Scripts/DungeonEscapeEnvController.cs`、`PushAgentEscape.cs`、`SimpleNPC.cs`
- **任务**：一队智能体合作——需有队员"牺牲"打败巨龙（NPC）掉出钥匙，其他队员捡钥匙开门逃脱。
- **结构**：单个 `SimpleMultiAgentGroup` 管理整队；`UnlockDoor()` 给整队 +1 并结束团队回合；死亡队员 `SetActive(false)`（**吸收态/absorbing state**）。
- **核心亮点**：典型的**协作多智能体 + 牺牲策略**，配套 `config/poca/DungeonEscape.yaml`（MA-POCA 专为带吸收态的协作设计）。`SimpleNPC` 是脚本化（非学习）的巨龙。

### 15. Sorter — 排序（变长观测 / 注意力）
- **文件**：`Sorter/Scripts/SorterAgent.cs`、`NumberTile.cs`
- **任务**：按数字从小到大依次碰触数量随机（1~20）的编号瓷砖。
- **核心亮点 — `BufferSensorComponent`**：每个可见瓷砖编码为 23 维（20 维数字 one-hot + 2 维相对位置 + 1 维是否已访问），通过 `AppendObservation` 喂给**变长缓冲传感器**，Python 端用**注意力机制（Attention）**处理不定数量实体。
- **动作**：离散 3 分支移动。
- **奖励**：碰对 +1，碰错 -1 并结束，每步 `-1/MaxStep`。
- **配套**：`config/ppo/Sorter_curriculum.yaml`，用课程逐步增加 `num_tiles` 难度。

### 16. Match3 — 三消游戏（自定义 Sensor/Actuator 集成）
- **文件**：`Match3/Scripts/Match3Agent.cs`、`Match3Board.cs`、`Match3ExampleActuator(Component).cs`、`Match3TileSelector.cs`
- **任务**：玩三消（消除棋盘上的同色块得分）。
- **核心亮点**：使用 **extensions 包中的 Match3 集成**（`Match3Sensor`/`Match3Actuator`/`AbstractBoard`），演示如何为**棋盘类离散问题**定制传感器与执行器及**动作掩码**（只允许合法交换）。
- **节拍控制**：训练时 `FastUpdate`（每步立即结算连锁消除并 `RequestDecision`），推理时 `AnimatedUpdate`（带动画状态机：FindMatches→ClearMatched→Drop→FillEmpty→WaitForMove）。因每步连锁数不同，**手动用 `MaxMoves` + `EpisodeInterrupted` 控制回合长度**而非 `MaxStep`。

### 17. Startup — 场景启动器
- **文件**：`Startup/Scripts/Startup.cs`：根据命令行参数加载指定示例场景，用于自动化/批量测试。

---

## 五、示例共性模式总结

通读全部示例后，可归纳出 ML-Agents 的标准开发范式：

1. **单 Agent 模式**（3DBall、GridWorld、PushBlock、Hallway、Pyramids、WallJump、Crawler、Walker、Worm）：
   继承 `Agent`，实现四件套；用 `EnvController`/`Area` 管理场景重置。

2. **多 Agent 模式**：
   - 竞争（FoodCollector）：各自独立奖励，共享区域。
   - 协作（DungeonEscape、PushBlockCollab）：`SimpleMultiAgentGroup` + 团队奖励。
   - 对抗团队 + 自博弈（Soccer）：双 Group + `TeamId` + ghost trainer。

3. **观测设计谱系**：向量（3DBall）→ 射线（PushBlock/Soccer）→ 视觉（Visual* / GridWorld）→ 变长缓冲（Sorter）→ 自定义（Basic/Match3）。

4. **动作设计谱系**：纯连续（3DBall/Crawler）→ 纯离散（GridWorld/PushBlock）→ 多分支离散（Soccer/WallJump）→ 混合（FoodCollector）。

5. **奖励设计谱系**：稠密塑形（Crawler 速度匹配）→ 步惩罚+终局（PushBlock/Hallway）→ 稀疏（Pyramids）→ 团队奖励（Soccer/DungeonEscape）。

6. **进阶训练技巧示例映射**：
   - 动作掩码 → GridWorld、Match3
   - 课程学习 → WallJump、Sorter
   - 环境随机化 → 3DBall、PushBlock
   - 记忆（LSTM） → Hallway
   - 内在奖励（Curiosity/RND） → Pyramids
   - 自博弈 → Soccer
   - 模仿学习（BC/GAIL，`.demo`） → 多数示例的 `Demos/` 目录

---

## 六、Python 训练后端（`ml-agents/mlagents/trainers`）

| 模块 | 作用 |
|------|------|
| `learn.py` / `cli_utils.py` | 训练入口 `mlagents-learn`，解析 YAML 配置 |
| `trainer_controller.py` | 顶层调度，管理多个 behavior 的训练循环 |
| `ppo/`、`sac/`、`poca/` | 三大 RL 算法实现（on-policy PPO、off-policy SAC、多智能体 MA-POCA） |
| `ghost/trainer.py` | 自博弈（Self-Play）包装器 |
| `torch_entities/networks.py` | 神经网络（编码器、Actor-Critic、注意力） |
| `torch_entities/components/reward_providers/` | 奖励信号：extrinsic、curiosity、rnd、gail |
| `torch_entities/components/bc/` | 行为克隆（BC） |
| `buffer.py` / `agent_processor.py` | 经验缓冲与轨迹处理 |
| `model_saver/torch_model_saver.py` + `model_serialization.py` | 导出 **ONNX** 模型供 Unity 推理 |
| `settings.py` | 配置 schema（与 `config/*.yaml` 对应） |

**训练数据流**：Unity（观测+奖励）→ gRPC → Python `agent_processor` → `buffer` → trainer 更新网络 → 周期性导出 ONNX → 回写 Unity。

### 训练配置（`config/`）
按算法分目录：`ppo/`、`sac/`、`poca/`、`imitation/`。每个 YAML 在 `behaviors:` 下以 **behavior 名（= `BehaviorParameters.BehaviorName`）** 为 key，定义：
- `trainer_type`、`hyperparameters`（batch_size/buffer_size/learning_rate...）
- `network_settings`（normalize/hidden_units/num_layers/vis_encode_type，记忆与注意力也在此）
- `reward_signals`（extrinsic/curiosity/rnd/gail 及其 strength/gamma）
- `max_steps`、`time_horizon`、`summary_freq`、`keep_checkpoints`
- 进阶：`environment_parameters`（课程/随机化）、`self_play`（自博弈）

---

## 七、端到端工作流程

```
                    ┌─────────────────────────────┐
                    │   config/<algo>/<Env>.yaml  │  超参数 / 课程 / 自博弈
                    └──────────────┬──────────────┘
                                   │ mlagents-learn
                    ┌──────────────▼──────────────┐
   导出 ONNX  ◄─────│  Python Trainer (PyTorch)   │
       │            │  PPO / SAC / POCA / Ghost    │
       │            └──────────────▲──────────────┘
       │                    gRPC   │  观测 / 动作 / 奖励
       │            ┌──────────────▼──────────────┐
       └──────────► │   Unity Runtime (C#)         │
       (推理)        │   Academy → Agent → Sensor   │
                    │   → Policy → Actuator        │
                    └──────────────────────────────┘
```

1. 在 Unity 中用 `Agent` + `BehaviorParameters` + Sensors/Actuators 搭建环境。
2. 用 `mlagents-learn config/ppo/3DBall.yaml --run-id=xxx` 启动 Python 训练，按提示运行 Unity（或可执行环境）。
3. 训练通过 gRPC 实时交互，TensorBoard 监控指标。
4. 训练完成导出 `.onnx` → 拖回 `BehaviorParameters.Model` → Unity 内本地推理（无需 Python）。

---

## 八、结论

本仓库是一个**结构清晰、分层明确**的"引擎侧 + 训练侧"双端机器学习框架：

- **C# 运行时**以 `Agent`/`Academy`/`Sensor`/`Actuator`/`Policy` 为核心抽象，组件化程度高，既可继承 `Agent` 快速开发，也可像 Basic 那样完全自定义组件。
- **18 个 Examples** 系统性地覆盖了从入门（3DBall）到前沿（Soccer 自博弈、DungeonEscape 协作、Sorter 变长注意力）的几乎所有 RL/MARL 场景与训练技巧，是学习该框架最佳的"活文档"。
- **Python 后端**实现了工业级的 PPO/SAC/POCA + 自博弈 + 模仿学习 + 内在奖励，并通过 ONNX 实现训练-推理解耦。

对于二次开发者，推荐学习路径：**3DBall（基础四件套）→ GridWorld（离散+掩码）→ PushBlock（射线感知）→ FoodCollector（混合动作/多智能体）→ Crawler（运动控制）→ Soccer（自博弈）/ DungeonEscape（协作）**。
```

